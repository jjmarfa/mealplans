import { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  components: {
    Form: {
      labelFontSize: 20,
    },
    Typography: {
      titleMarginBottom: 0,
    },
    Menu: {
      itemMarginInline: 10,
    },
  },
};

export default theme;
